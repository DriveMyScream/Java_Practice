package recursion;
import java.util.*;


public class subsencequences {
    public static void subSencequences(int arr[], int ind, int n, int list){
/*        if(ind>=n){
            System.out.println(list);
            return;
        }
        list.add(arr[ind]);
        subSencequences(arr, ind+1, n, list);
        list.remove(arr[ind]);
        subSencequences(arr, ind+1, n, list);
    }
    public static void main(String[] args) {
       ArrayList<Integer> list = new ArrayList<Integer>();
        int arr[] = {3, 1, 2};
        int n = arr.length-1;
        int ind = 0;
        subSencequences(arr, ind, n, list);   */

    }
}
